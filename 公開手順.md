# アヴァント コーポレートサイト 公開手順

このサイトは **Next.js（静的書き出し）** を **GitHub Pages** で公開し、
独自ドメイン **avant-neo.jp**（お名前.com取得）で表示します。

---

## 全体の流れ

1. ソースコードを GitHub のリポジトリに置く（push）
2. GitHub Pages を有効化（GitHub Actions が自動でビルド＆公開）
3. お名前.com の DNS をGitHub Pagesに向ける
4. ブラウザで `https://avant-neo.jp` を確認

> 一度この仕組みを作れば、**以後はファイルを修正して push するだけ**で自動的にサイトが更新されます。

---

## 1. GitHub への公開（初回のみ）

GitHub CLI（gh）の認証後、リポジトリ作成〜公開までは担当（Claude）が実行します。
あなたにお願いするのは **GitHubへのログイン（gh auth login）1回だけ** です。

### あなたの操作（1回だけ）

1. **新しい PowerShell ウィンドウ**を開く（PATH反映のため新規ウィンドウ必須）
2. 次のコマンドを実行：
   ```
   gh auth login
   ```
3. 表示される選択肢を次の通り選ぶ（↑↓キーで選び Enter）：
   - `GitHub.com`
   - `HTTPS`
   - `Login with a web browser`（ブラウザでログイン）
4. 画面に **ワンタイムコード**（例：`ABCD-1234`）が出るのでメモ
5. Enterを押すとブラウザが開く → GitHubにログイン → コードを貼り付け → 「Authorize」
6. 「Logged in as ◯◯◯」と出れば完了

完了したら担当に伝えてください。リポジトリ作成・push・Pages有効化は自動で行います。

---

## 2. お名前.com の DNS 設定（あなたの操作）

GitHub側の公開が終わったら、ドメインをGitHub Pagesに向けます。

### 手順

1. **お名前.com Navi** にログイン
   https://navi.onamae.com/
2. 上部メニュー **「ドメイン」→「DNS」** をクリック
3. 対象ドメイン **avant-neo.jp** を選び、**「DNSレコード設定」→「設定する」**
4. 以下の **Aレコードを4つ** 追加します。

   | ホスト名 | TYPE | VALUE（IPアドレス） | TTL |
   |---|---|---|---|
   | （空欄） | A | `185.199.108.153` | 3600 |
   | （空欄） | A | `185.199.109.153` | 3600 |
   | （空欄） | A | `185.199.110.153` | 3600 |
   | （空欄） | A | `185.199.111.153` | 3600 |

   - 「ホスト名」は **空欄**（＝ルートドメイン avant-neo.jp 本体）にします。
   - 1行ずつ「追加」ボタンで4行登録します。

5. （任意・推奨）`www`でもアクセスできるように、**CNAMEレコード**を1つ追加：

   | ホスト名 | TYPE | VALUE | TTL |
   |---|---|---|---|
   | `www` | CNAME | `neoavant2024-netizen.github.io.` | 3600 |

   ※ 末尾のドット `.` も込みで入力します（お名前.comでは自動で付く場合もあります）。

6. ページ下部の **「確認画面へ進む」→「設定する」** で保存
7. 反映を待つ（通常**数十分〜数時間**、最大72時間）

> ⚠️ 既存の `avant-neo.jp` のレコード（古いサーバ向けのAレコードやCNAME）が残っている場合は、
> **古いものを削除**してから上記を設定してください。重複すると正しく表示されません。

---

## 3. HTTPS（鍵マーク）を有効にする

DNSが反映されると、GitHub側で自動的に証明書が発行されます。
GitHubリポジトリの **Settings → Pages → 「Enforce HTTPS」** にチェックが入る状態になれば、
`https://avant-neo.jp` で安全に表示されます（チェックは担当が設定／確認します）。

---

## 4. 今後の更新方法

- 文章や色などを直したいときは、担当（Claude）にお伝えください。
  ファイルを修正 → push すると、**GitHub Actionsが自動でビルド＆再公開**します（数分で反映）。
- ご自身で確認したいときは、ローカルで以下を実行するとプレビューできます：
  ```
  cd avant-neo
  npm run dev
  ```
  → ブラウザで http://localhost:3000

---

## 補足：お問合せフォームについて

お問合せフォームは **Web3Forms**（無料・無制限のフォーム送信サービス）経由で
`info@avant-neo.jp` に届きます。GitHub Pages（静的サイト）でも問題なく動作します。

### 初期設定（1回だけ）
1. https://web3forms.com にアクセス
2. 「Access Key」欄に **info@avant-neo.jp** を入力して取得
3. info@avant-neo.jp 宛に届くメールから **Access Key** を確認
4. そのキーを担当（Claude）に伝える → `src/lib/site.ts` の `contactAccessKey` に設定して反映

※ Access Key はコードに直書きで問題ありません（送信先メールはサービス側で紐付くため、
キーが見えても第三者が送信先を変更することはできません）。
※ 送信元メールアドレスがHTMLに露出しないため、スパムに強い構成です。
